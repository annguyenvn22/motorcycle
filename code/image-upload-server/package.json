{
  "name": "image-upload-server",
  "version": "1.0.0",
  "description": "use multer for upload an image. use express for server. return image url when uploaded",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "annvc",
  "license": "ISC",
  "dependencies": {
    "ejs": "^2.3.4",
    "express": "^4.13.3",
    "multer": "^1.1.0"
  }
}
